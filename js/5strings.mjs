let name = "aarnav";
let surname = "dahal";

let fullName = `my name is ${name} and surname is ${surname}`;
//calling variables only works on `...` and will not work on "..." or '...' 

console.log(fullName);

console.log(`the sum of 1 + 1 is ${1 + 1}`);
// ${...} allows any and all javascript operations to take place inside it